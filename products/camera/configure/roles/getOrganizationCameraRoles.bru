meta {
  name: getOrganizationCameraRoles
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/organizations/:organizationId/camera/roles
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List all the roles in this organization
}
