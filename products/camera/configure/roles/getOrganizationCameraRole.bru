meta {
  name: getOrganizationCameraRole
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/organizations/:organizationId/camera/roles/:roleId
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  roleId: {{roleId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Retrieve a single role.
}
