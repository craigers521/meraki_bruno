meta {
  name: getNetworkWirelessAlternateManagementInterface
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/wireless/alternateManagementInterface
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return alternate management interface and devices with IP assigned
}
