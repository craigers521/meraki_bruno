meta {
  name: getNetworkWirelessBilling
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/wireless/billing
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the billing settings of this network
}
