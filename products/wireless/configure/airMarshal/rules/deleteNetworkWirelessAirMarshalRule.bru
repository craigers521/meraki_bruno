meta {
  name: deleteNetworkWirelessAirMarshalRule
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/networks/:networkId/wireless/airMarshal/rules/:ruleId
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  ruleId: {{ruleId}}
  basePath: {{basePath}}
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Delete an Air Marshal rule.
}
