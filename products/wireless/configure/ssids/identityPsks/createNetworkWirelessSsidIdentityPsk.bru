meta {
  name: createNetworkWirelessSsidIdentityPsk
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/networks/:networkId/wireless/ssids/:number/identityPsks
  body: json
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  number: {{number}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "name": "<string>",
    "groupPolicyId": "<string>",
    "passphrase": "<string>",
    "expiresAt": "<dateTime>"
  }
}

docs {
  Create an Identity PSK
}
