meta {
  name: getNetworkWirelessAirMarshal
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/wireless/airMarshal
  body: none
  auth: bearer
}

params:query {
  ~t0: {{t0}}
  ~timespan: {{timespan}}
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List Air Marshal scan results from a network
}
