meta {
  name: getNetworkWirelessMeshStatuses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/wireless/meshStatuses
  body: none
  auth: bearer
}

params:query {
  ~perPage: {{perPage}}
  ~startingAfter: {{startingAfter}}
  ~endingBefore: {{endingBefore}}
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List wireless mesh statuses for repeaters
}
