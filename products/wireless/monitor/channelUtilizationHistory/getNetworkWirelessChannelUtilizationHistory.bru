meta {
  name: getNetworkWirelessChannelUtilizationHistory
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/wireless/channelUtilizationHistory
  body: none
  auth: bearer
}

params:query {
  ~t0: {{t0}}
  ~t1: {{t1}}
  ~timespan: {{timespan}}
  ~resolution: {{resolution}}
  ~autoResolution: {{autoResolution}}
  ~clientId: {{clientId}}
  ~deviceSerial: {{deviceSerial}}
  ~apTag: {{apTag}}
  ~band: {{band}}
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return AP channel utilization over time for a device or network client
}
