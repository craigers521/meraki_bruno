meta {
  name: getOrganizationCellularGatewayEsimsInventory
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/organizations/:organizationId/cellularGateway/esims/inventory
  body: none
  auth: bearer
}

params:query {
  ~eids: {{eids}}
  ~eids: {{eids}}
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  The eSIM inventory of a given organization.
}
