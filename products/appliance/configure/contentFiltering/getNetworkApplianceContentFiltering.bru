meta {
  name: getNetworkApplianceContentFiltering
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/contentFiltering
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the content filtering settings for an MX network
}
