meta {
  name: swapNetworkApplianceWarmSpare
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/networks/:networkId/appliance/warmSpare/swap
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Swap MX primary and warm spare appliances
}
