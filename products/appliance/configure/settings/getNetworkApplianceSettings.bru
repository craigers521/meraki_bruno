meta {
  name: getNetworkApplianceSettings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/settings
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the appliance settings for a network
}
