meta {
  name: getNetworkApplianceSingleLan
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/singleLan
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return single LAN configuration
}
