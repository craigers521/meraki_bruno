meta {
  name: getNetworkApplianceRfProfile
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/rfProfiles/:rfProfileId
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  rfProfileId: {{rfProfileId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return a RF profile
}
