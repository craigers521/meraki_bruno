meta {
  name: getNetworkApplianceTrafficShaping
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/trafficShaping
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Display the traffic shaping settings for an MX network
}
