meta {
  name: createNetworkApplianceTrafficShapingCustomPerformanceClass
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/networks/:networkId/appliance/trafficShaping/customPerformanceClasses
  body: json
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "name": "<string>",
    "maxLatency": "<integer>",
    "maxJitter": "<integer>",
    "maxLossPercentage": "<integer>"
  }
}

docs {
  Add a custom performance class for an MX network
}
