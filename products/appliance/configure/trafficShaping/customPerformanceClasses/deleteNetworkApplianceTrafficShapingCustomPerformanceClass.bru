meta {
  name: deleteNetworkApplianceTrafficShapingCustomPerformanceClass
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/networks/:networkId/appliance/trafficShaping/customPerformanceClasses/:customPerformanceClassId
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  customPerformanceClassId: {{customPerformanceClassId}}
  basePath: {{basePath}}
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Delete a custom performance class from an MX network
}
