meta {
  name: deleteNetworkApplianceVlan
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/networks/:networkId/appliance/vlans/:vlanId
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  vlanId: {{vlanId}}
  basePath: {{basePath}}
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Delete a VLAN from a network
}
