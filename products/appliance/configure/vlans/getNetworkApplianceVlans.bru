meta {
  name: getNetworkApplianceVlans
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/vlans
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List the VLANs for an MX network
}
