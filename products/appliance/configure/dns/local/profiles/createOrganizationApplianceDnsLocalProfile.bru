meta {
  name: createOrganizationApplianceDnsLocalProfile
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/organizations/:organizationId/appliance/dns/local/profiles
  body: json
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "name": "<string>"
  }
}

docs {
  Create a new local DNS profile
}
