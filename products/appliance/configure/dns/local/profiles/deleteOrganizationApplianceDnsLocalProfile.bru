meta {
  name: deleteOrganizationApplianceDnsLocalProfile
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/organizations/:organizationId/appliance/dns/local/profiles/:profileId
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  profileId: {{profileId}}
  basePath: {{basePath}}
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Deletes a local DNS profile
}
