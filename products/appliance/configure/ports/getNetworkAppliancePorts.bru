meta {
  name: getNetworkAppliancePorts
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/ports
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List per-port VLAN settings for all ports of a MX.
}
