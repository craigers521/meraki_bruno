meta {
  name: getNetworkApplianceSsid
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/appliance/ssids/:number
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  number: {{number}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return a single MX SSID
}
