meta {
  name: getNetworkSensorMqttBrokers
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/networks/:networkId/sensor/mqttBrokers
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List the sensor settings of all MQTT brokers for this network. To get the brokers themselves, use /networks/{networkId}/mqttBrokers.
}
