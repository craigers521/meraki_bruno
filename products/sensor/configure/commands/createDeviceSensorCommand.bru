meta {
  name: createDeviceSensorCommand
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/devices/:serial/sensor/commands
  body: json
  auth: bearer
}

params:path {
  serial: {{serial}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "operation": "<string>"
  }
}

docs {
  Sends a command to a sensor
}
