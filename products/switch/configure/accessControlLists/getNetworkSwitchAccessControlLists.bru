meta {
  name: getNetworkSwitchAccessControlLists
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/switch/accessControlLists
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the access control lists for a MS network
}
