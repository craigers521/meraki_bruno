meta {
  name: getNetworkSwitchStormControl
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/switch/stormControl
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the storm control configuration for a switch network
}
