meta {
  name: getNetworkSwitchRoutingMulticast
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/switch/routing/multicast
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return multicast settings for a network
}
