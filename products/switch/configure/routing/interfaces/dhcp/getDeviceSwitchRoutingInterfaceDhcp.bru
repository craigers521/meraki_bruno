meta {
  name: getDeviceSwitchRoutingInterfaceDhcp
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/devices/:serial/switch/routing/interfaces/:interfaceId/dhcp
  body: none
  auth: bearer
}

params:path {
  serial: {{serial}}
  interfaceId: {{interfaceId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return a layer 3 interface DHCP configuration for a switch
}
