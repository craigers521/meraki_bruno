meta {
  name: getNetworkSwitchAlternateManagementInterface
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/switch/alternateManagementInterface
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the switch alternate management interface for the network
}
