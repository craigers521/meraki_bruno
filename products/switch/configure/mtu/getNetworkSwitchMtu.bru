meta {
  name: getNetworkSwitchMtu
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/switch/mtu
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the MTU configuration
}
