meta {
  name: addNetworkSwitchStack
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/networks/:networkId/switch/stacks/:switchStackId/add
  body: json
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  switchStackId: {{switchStackId}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "serial": "<string>"
  }
}

docs {
  Add a switch to a stack
}
