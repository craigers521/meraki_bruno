meta {
  name: deleteNetworkSwitchStackRoutingStaticRoute
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/networks/:networkId/switch/stacks/:switchStackId/routing/staticRoutes/:staticRouteId
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  switchStackId: {{switchStackId}}
  staticRouteId: {{staticRouteId}}
  basePath: {{basePath}}
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Delete a layer 3 static route for a switch stack
}
