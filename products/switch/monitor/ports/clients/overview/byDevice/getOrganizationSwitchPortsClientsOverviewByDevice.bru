meta {
  name: getOrganizationSwitchPortsClientsOverviewByDevice
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/organizations/:organizationId/switch/ports/clients/overview/byDevice
  body: none
  auth: bearer
}

params:query {
  ~t0: {{t0}}
  ~timespan: {{timespan}}
  ~perPage: {{perPage}}
  ~startingAfter: {{startingAfter}}
  ~endingBefore: {{endingBefore}}
  ~configurationUpdatedAfter: {{configurationUpdatedAfter}}
  ~mac: {{mac}}
  ~macs: {{macs}}
  ~macs: {{macs}}
  ~name: {{name}}
  ~networkIds: {{networkIds}}
  ~networkIds: {{networkIds}}
  ~portProfileIds: {{portProfileIds}}
  ~portProfileIds: {{portProfileIds}}
  ~serial: {{serial}}
  ~serials: {{serials}}
  ~serials: {{serials}}
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List the number of clients for all switchports with at least one online client in an organization.
}
