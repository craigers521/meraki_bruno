meta {
  name: getNetworkSmDeviceNetworkAdapters
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/sm/devices/:deviceId/networkAdapters
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  deviceId: {{deviceId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List the network adapters of a device
}
