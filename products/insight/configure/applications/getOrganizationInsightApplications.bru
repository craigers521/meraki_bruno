meta {
  name: getOrganizationInsightApplications
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/organizations/:organizationId/insight/applications
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List all Insight tracked applications
}
