meta {
  name: getNetworkTrafficShapingDscpTaggingOptions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/trafficShaping/dscpTaggingOptions
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Returns the available DSCP tagging options for your traffic shaping rules.
}
