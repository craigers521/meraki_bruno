meta {
  name: getNetworkHealthAlerts
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/health/alerts
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return all global alerts on this network
}
