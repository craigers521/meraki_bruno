meta {
  name: getNetworkSnmp
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/snmp
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the SNMP settings for a network
}
