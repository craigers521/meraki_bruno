meta {
  name: getOrganizationInventoryDevice
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/organizations/:organizationId/inventory/devices/:serial
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  serial: {{serial}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return a single device from the inventory of an organization
}
