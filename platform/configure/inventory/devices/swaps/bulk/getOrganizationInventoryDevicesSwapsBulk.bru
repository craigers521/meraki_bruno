meta {
  name: getOrganizationInventoryDevicesSwapsBulk
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/organizations/:organizationId/inventory/devices/swaps/bulk/:id
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  id: 
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List of device swaps for a given request ID ({id}).
}
