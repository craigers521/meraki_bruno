meta {
  name: getNetworkSettings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/settings
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the settings for a network
}
