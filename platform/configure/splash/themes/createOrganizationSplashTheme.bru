meta {
  name: createOrganizationSplashTheme
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/organizations/:organizationId/splash/themes
  body: json
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "name": "<string>",
    "baseTheme": "<string>"
  }
}

docs {
  Create a Splash Theme
}
