meta {
  name: deleteOrganizationSplashTheme
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/organizations/:organizationId/splash/themes/:id
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  id: 
  basePath: {{basePath}}
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Delete a Splash Theme
}
