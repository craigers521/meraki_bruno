meta {
  name: getOrganizationEarlyAccessFeaturesOptIns
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/organizations/:organizationId/earlyAccess/features/optIns
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List the early access feature opt-ins for an organization
}
