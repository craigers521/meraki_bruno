meta {
  name: getOrganizationEarlyAccessFeatures
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/organizations/:organizationId/earlyAccess/features
  body: none
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List the available early access features for organization
}
