meta {
  name: getNetworkNetflow
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/networks/:networkId/netflow
  body: none
  auth: bearer
}

params:path {
  networkId: {{networkId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return the NetFlow traffic reporting settings for a network
}
