meta {
  name: getAdministeredIdentitiesMeApiKeys
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/administered/identities/me/api/keys
  body: none
  auth: bearer
}

params:path {
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List the non-sensitive metadata associated with the API keys that belong to the user
}
