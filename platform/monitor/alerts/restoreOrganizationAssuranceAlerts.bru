meta {
  name: restoreOrganizationAssuranceAlerts
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/organizations/:organizationId/assurance/alerts/restore
  body: json
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "alertIds": [
      "<string>",
      "<string>"
    ]
  }
}

docs {
  Restore health alerts from dismissed
}
