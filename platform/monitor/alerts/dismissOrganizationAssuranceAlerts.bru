meta {
  name: dismissOrganizationAssuranceAlerts
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/organizations/:organizationId/assurance/alerts/dismiss
  body: json
  auth: bearer
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "alertIds": [
      "<string>",
      "<string>"
    ]
  }
}

docs {
  Dismiss health alerts
}
