meta {
  name: getOrganizationAssuranceAlerts
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/organizations/:organizationId/assurance/alerts
  body: none
  auth: bearer
}

params:query {
  ~perPage: {{perPage}}
  ~startingAfter: {{startingAfter}}
  ~endingBefore: {{endingBefore}}
  ~sortOrder: {{sortOrder}}
  ~networkId: {{networkId}}
  ~severity: {{severity}}
  ~types: {{types}}
  ~types: {{types}}
  ~tsStart: {{tsStart}}
  ~tsEnd: {{tsEnd}}
  ~category: {{category}}
  ~sortBy: {{sortBy}}
  ~serials: {{serials}}
  ~serials: {{serials}}
  ~deviceTypes: {{deviceTypes}}
  ~deviceTypes: {{deviceTypes}}
  ~deviceTags: {{deviceTags}}
  ~deviceTags: {{deviceTags}}
  ~active: {{active}}
  ~dismissed: {{dismissed}}
  ~resolved: {{resolved}}
  ~suppressAlertsForOfflineNodes: {{suppressAlertsForOfflineNodes}}
}

params:path {
  organizationId: {{organizationId}}
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return all health alerts for an organization
}
