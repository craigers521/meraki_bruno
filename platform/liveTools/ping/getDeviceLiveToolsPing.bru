meta {
  name: getDeviceLiveToolsPing
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/devices/:serial/liveTools/ping/:id
  body: none
  auth: bearer
}

params:path {
  serial: {{serial}}
  id: 
  basePath: {{basePath}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Return a ping job. Latency unit in response is in milliseconds. Size is in bytes.
}
