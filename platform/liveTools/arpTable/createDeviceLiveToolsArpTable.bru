meta {
  name: createDeviceLiveToolsArpTable
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/devices/:serial/liveTools/arpTable
  body: json
  auth: bearer
}

params:path {
  serial: {{serial}}
  basePath: {{basePath}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "callback": {
      "url": "<string>",
      "sharedSecret": "<string>",
      "httpServer": {
        "id": "<string>"
      },
      "payloadTemplate": {
        "id": "<string>"
      }
    }
  }
}

docs {
  Enqueue a job to perform a ARP table request for the device. This endpoint currently supports switches. This endpoint has a sustained rate limit of one request every five seconds per device, with an allowed burst of five requests.
}
